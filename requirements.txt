# Uncomment to enable Azure Monitor OpenTelemetry
# Ref: aka.ms/functions-azure-monitor-python
# azure-monitor-opentelemetry

# Ben K. would like to use pipenv instead of venv.
# But upon deploy, Azure will look at requirements.txt in order to install stuff.
# If we go with Azure in the long run, we might want to use venv exclusively.

azure-functions
requests
pydantic
python-dotenv
openpyxl
azure-communication-email
